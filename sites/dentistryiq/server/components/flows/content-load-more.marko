import { getAsArray, getAsObject } from "@base-cms/object-path";
import GAM from "../../../config/gam";

$ const { aliases } = input;
$ const nodes = getAsArray(input, "nodes");

<if(nodes.length)>
  <website-content-card-deck-flow nodes=nodes.slice(0, 10) aliases=aliases native-x=input.nativeX />
</if>
